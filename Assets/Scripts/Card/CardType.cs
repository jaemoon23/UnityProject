/// <summary>
/// LCB: Level-up card type definition
/// Issue #139 - Level-up card selection UI system
/// </summary>
public enum CardType
{
    Character = 0,  // Acquire new character
    Stat = 1,       // Increase stats
    Buff = 2,       // Positive effect
    Debuff = 3,     // Negative effect
    Skill = 4       // Acquire skill
}
