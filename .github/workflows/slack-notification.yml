# This workflow is disabled
# Use issue-to-slack-gemini.yml instead

name: Slack Notification (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: echo "This workflow is disabled. Use issue-to-slack-gemini.yml instead."
